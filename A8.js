{
    const idade = 11
    const patologia = false
    const altura = 160
    const estudante = true
    if (idade >= 12 && patologia === false && altura >= 150 && estudante === true){
        console.log ("R$ 10,00")
    } else {
        console.log ("ACESSO NEGADO")
    }
    const years = 12
    const doenca = false
    const tamanho = 165
    const meia = true
    if (years >= 12 && doenca === false && tamanho >= 150 && meia === true){
        console.log ("R$ 10,00")
    } else {
        console.log ("ACESSO NEGADO")
    }
}