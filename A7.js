{
    const aposentadoria = false
    const deficiencia = false
    const rendimentos = 1800000
    if (aposentadoria === false && deficiencia === false && rendimentos <2855970){
        console.log ("ISENTO")
    } 
}
{
    const aps = true
    const def = true
    const rend = 3000000
    if (aps === true && def === true && rend >2855970){
        console.log ("PEGA LEÃO")
    } 
}